{
  "name": "careconnectcourier-server",
  "version": "1.0.0",
  "description": "Express backend serving JSON data for CareConnectCourier (mock DB)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install"  // Add this build script if needed
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.1",
    "stripe": "^12.0.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.3.1",
    "helmet": "^6.0.1",
    "express-rate-limit": "^6.8.0"
  },
  "engines": {
    "node": "18.x"  // This is optional, specify if needed
  }
}

