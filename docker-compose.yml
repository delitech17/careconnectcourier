version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - node_server/.env
    restart: unless-stopped
    volumes:
      - ./node_server:/usr/src/app/node_server:ro
